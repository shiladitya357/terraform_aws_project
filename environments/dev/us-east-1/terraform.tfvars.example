# Optional EC2 key pair name for emergency SSH access.
# key_name = "my-key-pair"

# Do not commit the real value. Consider TF_VAR_db_password in your shell instead.
db_password = "replace-with-a-strong-password"
